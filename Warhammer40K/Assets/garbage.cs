using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class garbage : MonoBehaviour
{
    CanvasRenderer cvr;
    // Start is called before the first frame update
    void Start()
    {
        cvr = GetComponent<CanvasRenderer>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
